{
  "name": "smartops-json-server",
  "version": "1.0.0",
  "description": "JSON Server for SmartOps project",
  "main": "db.json",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT 8080 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": ">=14"
  }
}
